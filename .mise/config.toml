[settings]
lockfile = true

[tools]
"go:golang.org/x/tools/cmd/goimports" = "latest"
"go:golang.org/x/tools/gopls" = "latest"
"go:mvdan.cc/gofumpt" = "latest"
go = "latest"
uv = "latest"
"pipx:robotcode" = { version = "latest", extras = "all" }
"pipx:robotframework-robocop" = "latest"
"pipx:robotframework" = "latest"
golangci-lint = "latest"

[tasks.tests]
depends = ["unit-tests", "e2e-tests"]

[tasks.build]
run = "go build ./cmd/tmux-rr"

[tasks.unit-tests]
run = "go test -v ./..."

[tasks.e2e-image-build]
run = "podman build -t tmux-rr-test -f e2e/Containerfile e2e"

[tasks.e2e-tests]
depends = ["build", "e2e-image-build"]
run = "robot --loglevel TRACE -- e2e/tests.robot"

[tasks.e2e-container]
depends = ["build", "e2e-image-build"]
run = "podman run --cap-add SYS_ADMIN -it --rm -v .:/wd -w /wd tmux-rr-test"
